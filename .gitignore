# Secrets — never commit
.env
credentials.json

# Python
__pycache__/
*.pyc
.pytest_cache/

# Runtime state (personal data, changes constantly)
memory/

# Vision models — auto-downloaded on first use (see vision.py)
models/

# Scratch / one-off files not part of the project
btc_predictions.html
